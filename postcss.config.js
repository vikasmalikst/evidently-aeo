export default {
  plugins: {
    '@tailwindcss/postcss': {},
    autoprefixer: {}, // autoprefixer is usually included in @tailwindcss/postcss but keeping it explicit if needed or just replace with simplified config
  },
};
